export type InputRootProps = {
  label?: string;
  required?: boolean;
};

export type ContactFormInputs = {
  firstName?: string;
  lastName?: string;
  email: string;
  phoneNumber: string;
  topic?: string;
  message: string;
};
